function selection_sort(a){
  for(var x=0; x<a.length-1; x++){
    var min = x
    for(var y=x+1; y<a.length; y++){
      if(a[min]>a[y]) min=y
    }
    if(x!=min){
      [a[x],a[min]]=[a[min],a[x]]
    }
  }
}
