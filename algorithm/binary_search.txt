function binary_search(a,key){
  var lo = 0
  var hi= a.length-1
debugger
  while(lo <= hi){
    var mid = lo + ~~((hi-lo)/2)
    if     (key < a[mid]) hi= mid-1
    else if  (key > a[mid]) lo= mid+1
    else    return a[mid]
  }
  return -1
}
